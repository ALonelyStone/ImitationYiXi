<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_login"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@mipmap/sign_bg3"
    android:orientation="vertical"
    tools:context="stone.imitationyixi.mvp.login.view.activity.LoginActivity">

    <include layout="@layout/widget_toolbar"/>

    <LinearLayout
        android:id="@+id/btn_ll"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/login"
            android:layout_width="0dp"
            android:layout_height="50dp"
            android:layout_margin="@dimen/login_btn"
            android:layout_weight="1"
            android:background="@drawable/btn_login_ripple"
            android:gravity="center"
            android:text="登录"
            android:textColor="@android:color/white"
            android:textSize="@dimen/login_btn_text"/>

        <TextView
            android:id="@+id/regist"
            android:layout_width="0dp"
            android:layout_height="50dp"
            android:layout_margin="@dimen/login_btn"
            android:layout_weight="1"
            android:background="@drawable/btn_login_ripple"
            android:gravity="center"
            android:text="注册"
            android:textColor="@android:color/white"
            android:textSize="@dimen/login_btn_text"/>

    </LinearLayout>

    <FrameLayout
        android:id="@+id/login_panel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

</LinearLayout>
